[pytest]
pythonpath = src
markers =
    pre_deploy: Tests executing before deployment (hygiene, unit, basic integration)
    pre_actuation: Tests executing before state change (adversarial, property-based boundary checks)
    post_incident: Tests executing post-incident (recovery gates, rollbacks, and metrics regression)
