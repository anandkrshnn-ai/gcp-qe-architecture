Terraform used the selected providers to generate the following execution plan. Resource actions are indicated with the following symbols:
  + create

Terraform will perform the following actions:

  # module.cloud_run.google_cloud_run_service.service will be created
  + resource "google_cloud_run_service" "service" {
      + id       = (known after apply)
      + location = "asia-south1"
      + name     = "sandbox-cloud-run"
      + project  = "mock-project-id"
      + status   = (known after apply)
      # ... (omitting details for brevity)
    }

  # module.cloud_sql.google_sql_database_instance.main will be created
  + resource "google_sql_database_instance" "main" {
      + connection_name     = (known after apply)
      + database_version    = "POSTGRES_16"
      + deletion_protection = false
      + id                  = (known after apply)
      + name                = "sandbox-cloudsql"
      + region              = "asia-south1"
      # ...
    }

  # module.gke.google_container_cluster.primary will be created
  + resource "google_container_cluster" "primary" {
      + endpoint                 = (known after apply)
      + id                       = (known after apply)
      + location                 = "asia-south1"
      + name                     = "sandbox-gke-cluster"
      + network                  = (known after apply)
      + remove_default_node_pool = true
      + subnetwork               = (known after apply)
      # ...
    }

  # module.vpc.google_compute_network.vpc will be created
  + resource "google_compute_network" "vpc" {
      + auto_create_subnetworks         = false
      + delete_default_routes_on_create = true
      + id                              = (known after apply)
      + name                            = "sandbox-vpc"
      + project                         = "mock-project-id"
      # ...
    }

Plan: 7 to add, 0 to change, 0 to destroy.

─────────────────────────────────────────────────────────────────────────────

Note: You didn't use the -out option to save this plan, so Terraform can't guarantee to take exactly these actions if you run "terraform apply" now.
